<table>
  <thead>
    <tr>
      <th>Tournament name:</th>
      <th>Started at:</th>
    </tr>
  </thead>
  <tbody>
    <% @tournaments.each do |tournament| %>
      <tr>
        <td><%= tournament.name %></td>
        <td><%= tournament.started_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>
