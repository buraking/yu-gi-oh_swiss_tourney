<h3>This is <%= @tournament.name %></h3>

<%= form_for Participant.new(tournament_id: @tournament.id, user_id: current_user.id) do |f| %>
  <%= f.hidden_field :tournament_id %>
  <%= f.hidden_field :user_id %>
  <%= f.submit 'subscribe' %>
<% end %>
